% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_analysis.R
\name{create_custom_bounds}
\alias{create_custom_bounds}
\title{Create Custom Parameter Bounds}
\usage{
create_custom_bounds(
  parameters,
  min,
  max,
  modification_type,
  default = NULL,
  description = NULL,
  category = NULL,
  sample_min = NULL,
  sample_max = NULL
)
}
\arguments{
\item{parameters}{Vector of parameter names (e.g., c("BETA", "CTMAX", "TAB1"))}

\item{min}{Vector of minimum physical values (same length as parameters)}

\item{max}{Vector of maximum physical values (same length as parameters)}

\item{modification_type}{Vector of modification types (same length as parameters):
\itemize{
\item "relchg" - Relative change (scales original value to match sampled target relative to default)
\item "abschg" - Absolute change (shifts original value to match sampled target relative to default)
}}

\item{default}{Vector of default values (required for determining modification factors)}

\item{description}{Vector of parameter descriptions (optional)}

\item{category}{Vector of parameter categories (optional)}

\item{sample_min}{Vector of minimum sampling values (defaults to physical min)}

\item{sample_max}{Vector of maximum sampling values (defaults to physical max)}
}
\value{
Tibble with parameter bounds containing columns: parameter, description, min, max,
default, modification_type, category, sample_min, sample_max
}
\description{
Create parameter bounds manually without using CSV file. This function defines
the sampling ranges and modification types for sensitivity analysis parameters.
}
\examples{
\dontrun{
# Create custom bounds for 3 parameters
custom_bounds <- create_custom_bounds(
  parameters = c("BETA", "CTMAX", "TAB1"),
  min = c(0.1, 2, 0.1),
  max = c(10, 12, 5),
  modification_type = c("relchg", "relchg", "abschg"),
  default = c(4.5, 5, 1)
)

# Use in sensitivity analysis
sobol_samples <- generate_sobol_samples(
  par_bounds = create_sobol_bounds(custom_bounds),
  n = 100
)
}
}
