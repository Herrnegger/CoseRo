% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosero_readers.R
\name{write_cosero_parameters}
\alias{write_cosero_parameters}
\title{Write COSERO Parameter File}
\usage{
write_cosero_parameters(
  par_file,
  param_data,
  project_info = NULL,
  add_timestamp = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{par_file}{Path to the output parameter file}

\item{param_data}{Data frame with parameter values (from read_cosero_parameters or modified)}

\item{project_info}{Character string for the first line of the file. If NULL (default),
reads the existing first line from par_file (if it exists) or uses a generic header.}

\item{add_timestamp}{Logical. If TRUE (default), appends a timestamp to the project_info.}

\item{quiet}{Suppress messages}
}
\value{
Invisible NULL (called for side effects)
}
\description{
Writes a parameter data frame to a COSERO parameter file in tabular format.
Automatically adds a timestamp to track when the file was last modified.
}
\examples{
\dontrun{
# Read, modify, and write
params <- read_cosero_parameters("para.txt")
params$BETA_ <- params$BETA_ * 1.1
write_cosero_parameters("para_modified.txt", params)

# Write with custom project info
write_cosero_parameters("para.txt", params,
                        project_info = "Wildalpen optimized")

# Write without timestamp
write_cosero_parameters("para.txt", params, add_timestamp = FALSE)
}
}
