% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosero_map.R
\name{write_station_metrics_csv}
\alias{write_station_metrics_csv}
\title{Write per-subbasin metrics + coordinates to a CSV (for spatial clustering)}
\usage{
write_station_metrics_csv(
  station_metrics,
  stations,
  id_field,
  catchments,
  catchment_id_field,
  cache_dir,
  spinup = 0L
)
}
\arguments{
\item{station_metrics}{Data frame from \code{compute_all_station_metrics()}
(keyed by \code{suffix} = the output-column subbasin id).}

\item{stations, catchments}{sf layers (WGS84) or NULL.}

\item{id_field, catchment_id_field}{ID columns in the respective layers.}

\item{cache_dir}{Directory to write into (the fst cache dir).}

\item{spinup}{Spin-up timesteps excluded from the metrics (recorded as a col).}
}
\value{
Invisibly, the path to the written CSV (or NULL if nothing to write).
}
\description{
Joins the launch-time metrics (\code{compute_all_station_metrics()}) to one
WGS84 coordinate per subbasin: the gauging-station point when available,
otherwise the catchment representative point. Written to
\code{output/.cache/station_metrics.csv} at every launch. Columns:
subbasin, lon, lat, coord_source, NSE, KGE, r, alpha, beta, n, spinup.
}
\keyword{internal}
