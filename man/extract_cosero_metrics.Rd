% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_analysis.R
\name{extract_cosero_metrics}
\alias{extract_cosero_metrics}
\title{Extract Pre-calculated Metrics from COSERO Statistics}
\usage{
extract_cosero_metrics(
  ensemble_results,
  subbasin_id = "001",
  metric = "NSE",
  warn_nan = TRUE
)
}
\arguments{
\item{ensemble_results}{Results from run_cosero_ensemble() or run_cosero_ensemble_parallel()}

\item{subbasin_id}{Subbasin ID to extract metrics for (e.g., "001" or 1)}

\item{metric}{Performance metric to extract ("NSE", "KGE", "BIAS", "RMSE", etc.)
Must be a column name in the statistics data frame}

\item{warn_nan}{If TRUE, warns when metrics are NaN (no observed data for subbasin)}
}
\value{
Numeric vector of metric values for each ensemble run
}
\description{
Extracts performance metrics (NSE, KGE, etc.) that were pre-calculated by COSERO
and stored in statistics.txt. This is the recommended approach for standard metrics
as it is faster and guaranteed to match COSERO's internal evaluation (including
proper spin-up handling).
}
\examples{
\dontrun{
# Run ensemble
results <- run_cosero_ensemble_parallel(project_path, parameter_sets, n_cores = 4)

# Extract NSE for subbasin 001
nse_values <- extract_cosero_metrics(results, subbasin_id = "001", metric = "NSE")

# Use in sensitivity analysis
sobol_indices <- calculate_sobol_indices(nse_values, sobol_samples)
plot_sobol(sobol_indices)
}
}
