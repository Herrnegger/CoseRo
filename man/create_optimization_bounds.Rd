% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosero_optimize.R
\name{create_optimization_bounds}
\alias{create_optimization_bounds}
\title{Create Optimization Parameter Bounds}
\usage{
create_optimization_bounds(
  parameters,
  lower,
  upper,
  default = NULL,
  modification_type = NULL,
  description = NULL,
  category = NULL
)
}
\arguments{
\item{parameters}{Vector of parameter names}

\item{lower}{Lower bounds for optimization}

\item{upper}{Upper bounds for optimization}

\item{default}{Default parameter values (optional, uses midpoint if NULL)}

\item{modification_type}{"relchg" (relative) or "abschg" (absolute) for each parameter}

\item{description}{Parameter descriptions (optional)}

\item{category}{Parameter categories (optional, e.g., "runoff", "snow", "soil")}
}
\value{
Tibble with columns: parameter, description, min, max, default, modification_type, category
}
\description{
Defines parameter bounds for optimization. Matches sensitivity_analysis.R structure.
Compatible with inst/extdata/parameter_bounds.csv format.
}
\examples{
# Basic usage with automatic defaults
par_bounds <- create_optimization_bounds(
  parameters = c("BETA", "CTMAX"),
  lower = c(1, 2),
  upper = c(6, 8)
)

# Full specification with all metadata
par_bounds_full <- create_optimization_bounds(
  parameters = c("BETA", "CTMAX", "TCOR"),
  lower = c(1, 2, -1),
  upper = c(6, 8, 2),
  default = c(4.5, 5, 0),
  modification_type = c("relchg", "relchg", "abschg"),
  description = c(
    "Runoff generation parameter",
    "Maximum snow melt factor",
    "Temperature correction"
  ),
  category = c("runoff", "snow", "temperature")
)

# Load from standard parameter bounds file
\dontrun{
# Use existing bounds from package
full_bounds <- load_parameter_bounds()
# Subset for optimization
opt_params <- c("BETA", "CTMAX", "LP")
par_bounds <- full_bounds[full_bounds$parameter \%in\% opt_params, ]
}
}
