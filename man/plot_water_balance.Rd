% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_helpers.R
\name{plot_water_balance}
\alias{plot_water_balance}
\title{Create Water Balance Time Series Plot}
\usage{
plot_water_balance(data, selected_vars = NULL, show_cumulative = TRUE)
}
\arguments{
\item{data}{Data frame with Date and water balance columns}

\item{selected_vars}{Character vector of variable names to plot (default: c("BW0", "BW3", "SWW"))}

\item{show_cumulative}{Logical. If TRUE, calculates and shows cumulative flux variables.}
}
\value{
Plotly object
}
\description{
Creates an interactive plotly chart showing water balance components including
storage states (soil moisture, groundwater, snow) and cumulative fluxes (P, ET, Q).
}
\examples{
\dontrun{
# Create water balance plot with default variables
p <- plot_water_balance(sb_data$water_balance)

# Plot specific variables
p <- plot_water_balance(sb_data$water_balance,
                         selected_vars = c("BW0", "SWW", "P_cum"))

# Without cumulative variables
p <- plot_water_balance(sb_data$water_balance, show_cumulative = FALSE)
}
}
